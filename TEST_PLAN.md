# План тестирования Standoff History App

## 1. Подготовка окружения

Цель: убедиться, что приложение запускается на целевой машине.

1. Проверить версию Python:

```bash
python --version
```

Ожидаемо: Python 3.11 или новее.

2. Создать и активировать виртуальное окружение:

```bash
python -m venv .venv
.venv\Scripts\activate
```

3. Установить зависимости:

```bash
pip install -r requirements.txt
```

4. Проверить наличие основных файлов:

```text
main.py
config.json
requirements.txt
app/
tools/
content/
logs/
```

Ожидаемо: структура проекта на месте, ошибок установки нет.

## 2. Проверка контента

Цель: убедиться, что все нужные ролики и заглушки лежат в `content/`.

Обязательные файлы:

```text
standoff_2016.mp4
standoff_2017.mp4
standoff_2018.mp4
standoff_2019.mp4
standoff_2020.mp4
standoff_2021.mp4
standoff_2022.mp4
standoff_2023.mp4
standoff_2024.mp4
standoff_2025.mp4
standoff_2026.mp4
standoff_idle.mp4
standoff_before_2016.mp4
standoff_after_2026.mp4
```

Для `idle`, `before_2016`, `after_2026` допустимы также `.png`, `.jpg`, `.jpeg`.

Запуск проверки:

```bash
python tools/content_check.py
```

Ожидаемо:

```text
Status: OK
```

Если есть `missing`, `empty` или `unreadable`, нужно заменить или добавить указанные файлы.

## 3. Тест без Arduino

Цель: проверить GUI, fullscreen, ручной режим, debug и маппинг годов без железа.

Запуск:

```bash
python main.py --simulate
```

Проверить:

- приложение открылось;
- показывается idle;
- окно fullscreen, если в `config.json` стоит `"fullscreen": true`;
- курсор скрыт, если стоит `"hideCursor": true`;
- `F1` открывает и закрывает debug-панель;
- `F2` открывает и закрывает ручной режим.

Быстрые клавиши simulate:

```text
I -> idle
B -> before_2016
A -> after_2026
V -> 2017
```

Ожидаемо: каждый сценарий запускает соответствующий контент или понятную техническую ошибку, если файла нет.

## 4. Проверка ручного режима

Цель: убедиться, что зона может работать без Arduino.

Открыть ручной режим:

```text
F2
```

Проверить ввод `YY`:

| Ввод | Ожидаемый сценарий |
|---|---|
| `00` | `before_2016` |
| `01` | `before_2016` |
| `15` | `before_2016` |
| `16` | `standoff_2016.mp4` |
| `17` | `standoff_2017.mp4` |
| `26` | `standoff_2026.mp4` |
| `27` | `after_2026` |
| `42` | `after_2026` |
| `99` | `after_2026` |

Проверить кнопки:

- `Idle`;
- `Before 2016`;
- `After 2026`;
- ролики `2016-2026`;
- `RUN` -> команда `2`;
- `OFF` -> команда `0`;
- `IDLE` -> команда `0`;
- `ERROR` -> команда `0`.

Ожидаемо: ручной запуск работает даже без подключенной Arduino.

## 5. Проверка debug-панели

Цель: убедиться, что монтажник видит состояние системы.

Открыть:

```text
F1
```

Проверить поля:

- `Arduino status`;
- `COM-port`;
- `Last raw input`;
- `Candidate value`;
- `Stable value`;
- `Last triggered value`;
- `Mapped year`;
- `Current scenario`;
- `Current file`;
- `LED status`;
- `Last LED command`;
- `Last error`;
- `Content check status`.

Ожидаемо: значения обновляются при ручном запуске, simulate-командах и Serial-вводе.

## 6. Проверка Arduino / Serial

Цель: убедиться, что две Arduino отправляют корректный формат.

Поддерживаются:

```text
D   -> одна цифра 0-9, роль берётся из COM-порта
TD  -> десятки
UD  -> единицы
OD  -> единицы
```

Подключить обе Arduino по USB:

```text
Arduino десятков -> tensPort
Arduino единиц -> onesPort
```

Проверить список портов:

```bash
python tools/serial_check.py --mode dual_digit --tens-port auto --ones-port auto --baud 9600
```

Если auto перепутал платы или не нашёл порт, указать порты вручную:

```bash
python tools/serial_check.py --mode dual_digit --tens-port COM3 --ones-port COM4 --baud 9600
```

Ожидаемо:

```text
tens valid: 'T1'
ones valid: 'U7'
combined YY: 17
```

Некорректные значения должны отображаться как `invalid`:

```text
tens invalid: '12'
ones invalid: 'A'
```

## 7. Основной тест с Arduino

Цель: проверить полный сценарий барабан → Arduino → приложение → экран.

Запуск:

```bash
python main.py
```

Проверить:

1. На старте показывается idle.
2. Если Arduino не найдена, приложение не падает.
3. В debug видно `Arduino status`.
4. При изменении барабана приложение получает цифру десятков и цифру единиц с разных COM-портов.
5. Когда есть обе цифры, приложение собирает `YY`.
6. Значение стабилизируется через `stabilizationDelayMs`.
7. Полный год собирается как `20YY`.
8. Запускается правильный сценарий.
9. После завершения ролика приложение возвращается в idle.

## 8. Полный набор годовых сценариев

Проверить на барабане или через ручной режим:

| YY | Год | Ожидаемый результат |
|---|---:|---|
| `00` | 2000 | `before_2016` |
| `01` | 2001 | `before_2016` |
| `15` | 2015 | `before_2016` |
| `16` | 2016 | `standoff_2016.mp4` |
| `17` | 2017 | `standoff_2017.mp4` |
| `18` | 2018 | `standoff_2018.mp4` |
| `19` | 2019 | `standoff_2019.mp4` |
| `20` | 2020 | `standoff_2020.mp4` |
| `21` | 2021 | `standoff_2021.mp4` |
| `22` | 2022 | `standoff_2022.mp4` |
| `23` | 2023 | `standoff_2023.mp4` |
| `24` | 2024 | `standoff_2024.mp4` |
| `25` | 2025 | `standoff_2025.mp4` |
| `26` | 2026 | `standoff_2026.mp4` |
| `27` | 2027 | `after_2026` |
| `42` | 2042 | `after_2026` |
| `99` | 2099 | `after_2026` |

## 9. Проверка защиты от повторов

Цель: убедиться, что один и тот же год не перезапускается бесконечно.

Шаги:

1. Отправить `17`.
2. Дождаться запуска `2017.mp4`.
3. Повторно отправить `17` несколько раз.

Ожидаемо:

- ролик не перезапускается бесконечно;
- в логе есть `duplicate_ignored`;
- если затем отправить `18`, текущий контент меняется на `2018.mp4`.

## 10. Проверка ошибок

Цель: убедиться, что ошибки не ломают основной экран.

Проверить:

| Ситуация | Ожидаемое поведение |
|---|---|
| Одна или обе Arduino не подключены | idle, debug показывает disconnected, ручной режим работает |
| Arduino отключили во время работы | текущий ролик не падает, приложение пробует reconnect |
| Пришло `12` вместо одной цифры | значение с этой платы игнорируется, экран не меняется |
| Файл ролика отсутствует | техническая заглушка, ошибка в debug и логе |
| `idle` отсутствует | техническая заглушка, приложение не падает |

## 11. Проверка LED

Цель: проверить LED-команды без блокировки основного сценария.

Если LED пока не подключена, оставить:

```json
"led": {
  "enabled": false
}
```

Ожидаемо: приложение работает без ленты.

Если LED подключена через Arduino/Serial, включить и выбрать целевой порт:

```json
"serial": {
  "dual": {
    "ledTarget": "tens"
  }
},
"led": {
  "enabled": true
}
```

Проверить из ручного режима:

- `RUN` -> команда `2`;
- `OFF` -> команда `0`;
- `IDLE` -> команда `0`;
- `ERROR` -> команда `0`.

Проверить автоматический сценарий:

1. Отправить год `17`.
2. Ожидаемо: отправляется команда `2`.
3. Через `runDelayMs` запускается ролик.
4. Если `sendOffOnVideoStart=true`, отправляется команда `0`.
5. Отправить год вне диапазона, например `09` или `30`.
6. Ожидаемо: отправляется команда `1`, запускается `before_2016` или `after_2026`, затем через `outOfRangeDurationMs` отправляется `0`.

## 12. Проверка логов

Лог находится здесь:

```text
logs/events.log
```

Проверить, что логируются:

- запуск приложения;
- загрузка сценариев;
- подключение или отсутствие Arduino;
- валидные входящие значения;
- невалидные входящие значения;
- stable value;
- выбранный год;
- текущий файл;
- ручные запуски;
- LED-команды;
- ошибки контента;
- возврат в idle.

## 13. Финальный прогон перед площадкой

Чеклист:

- [ ] Python 3.11+ установлен.
- [ ] Зависимости установлены.
- [ ] `python tools/content_check.py` возвращает `Status: OK`.
- [ ] `python main.py --simulate` запускается.
- [ ] `F1` debug работает.
- [ ] `F2` manual работает.
- [ ] Все сценарии `00-99` критичного набора проверены.
- [ ] Обе Arduino определяются.
- [ ] `serial_check.py` показывает `tens valid`, `ones valid` и `combined YY`.
- [ ] `python main.py` работает с двумя Arduino.
- [ ] Повтор одного значения не перезапускает ролик.
- [ ] Новый год во время ролика переключает сценарий.
- [ ] После завершения ролика происходит возврат в idle.
- [ ] Ошибки контента не роняют приложение.
- [ ] LED-команды проверены или `led.enabled=false`.
- [ ] Логи пишутся в `logs/events.log`.
- [ ] На целевом ПК отключены сон, заставка, уведомления и автоотключение экрана.
